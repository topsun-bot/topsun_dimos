---
description: dimos 复杂主题文档模板（通俗→细致 + mermaid 多图 + 自动转 PDF）
alwaysApply: true
---

# dimos 文档模板（"dimos 模板"）

当用户说"**按 dimos 模板生成**"/"用 dimos 文档模板"/"按模板生成" 等时，遵循本规则。
范例参考：`jiangtao/cursor/dimos-navigation-mapping-tutorial.md` + `.pdf`。

## 文件落点

| 内容 | 位置 |
|------|------|
| Markdown 源文件 | `docs/cursor/<topic>.md`（kebab-case，比如 `dimos-perception-tutorial.md`） |
| PDF 输出 | 同目录同名 `.pdf` |
| PDF 生成命令 | `python3 .cursor/scripts/md_to_pdf.py docs/cursor/<topic>.md` |

写完 markdown 后**立刻调用脚本生成 PDF**，并验证页数 / 大小合理（用 `pdfinfo` 抽页用 `pdftoppm` 截 PNG 抽查关键页面，特别是 mermaid 图所在页）。

## 文档骨架（必须按此顺序）

```markdown
# <主标题：从零理解 X / 深入理解 X / X 完全指南>

> 一句话说"这是写给谁看的、需要什么前置知识"。
> 一句话说"基于 dimos <branch> 的 commit `<sha>`"。

---

## 目录

- [一、通俗篇：<主题>是怎么回事](#一通俗篇主题是怎么回事)
- [二、总览：<主题>的 N 层架构 / N 大模块](#二总览)
- [三、<第一大块>详解](#三...)
- ...
- [七、扩展点和延伸阅读](#七...)

---

# 一、通俗篇：<主题>是怎么回事

> **这一章 0 代码、0 框架、0 dimos 类名**，只讲领域概念，让没接触过的人能跟上。

## 1.1 为什么需要 X？（动机）
## 1.2 X 是什么 / 关键概念定义
## 1.3 X 的几种类型 / 几个层次（带 ASCII 示意图）
## 1.4 核心算法直观解释（不写代码，用类比）
## 1.5 控制 / 输出怎么落地

---

# 二、总览：<主题>的 N 层架构

> 完整大架构图（mermaid flowchart TB），每个模块画成节点，核心数据流标在线上。
> 紧跟一张"模块 → 输入 → 输出"的对照表。

```mermaid
flowchart TB
    ...
```

| 层 | 模块 | 输入 | 输出 |
|----|------|------|------|
| ... | ... | ... | ... |

---

# 三、<第一大块> — <通俗副标题>

## 3.1 第一站：<子模块> — <一句话职责>

文件：`<相对路径>`

**问题**：用大白话提出这个子模块要解决什么问题。
**答案**：用大白话给出方法。

（mermaid flowchart 展示该子模块的内部数据流 / 算法步骤）

代码片段（最小可读，5-15 行）：

```python
# 引用必要的、能说明问题的代码
```

参数 / 配置表：

| 字段 | 默认 | 含义 |
|------|------|------|

## 3.2 第二站：...
...

## 3.X 完整的"<某某>链"流程

> 本章末尾再画一张总图，把上面所有子模块串起来。

---

# 四、<第二大块> — <通俗副标题>

> 同样的结构：4.1/4.2/.../4.X，每节配 1+ 流程图。
> 涉及状态机用 `stateDiagram-v2`，涉及多模块交互用 `sequenceDiagram`。

---

# 五、<可选的进阶子主题>
（同 4 的结构）

---

# 六、端到端实战 — 用 <真实 blueprint> 做一次完整任务

## 6.1 启动命令 + blueprint 拓扑图（一张大 flowchart）
## 6.2 一次完整动作的时序图（sequenceDiagram，从用户输入到电机指令）
## 6.3 （可选）agent / LLM 介入场景的时序图

---

# 七、扩展点和延伸阅读

## 7.1 怎么自己写一个 <子组件>（含完整可粘贴代码）
## 7.2 怎么替换 <另一个子组件>
## 7.3 推荐学习资料
## 7.4 参数 cheatsheet

| 想做的事 | 看哪 |
|---------|------|
| 改 X | `Class.config.X`（默认 ...） |
| 调试 X | `export DEBUG_X=1` |

---

> 文档基于 `<commit-sha>`。后续 dev 同步后细节可能调整，但整体架构应保持稳定。
```

## 写作风格 13 条铁律

1. **第一章绝对通俗**：不出现 dimos 类名、不写 Python 代码，全是大白话+ASCII 图+类比
2. **从第二章开始引入框架**：先画总览图，再分模块讲，每讲一个先说"问题"再说"答案"
3. **章节标题用罗马数字（一、二、...）**，子节用阿拉伯（3.1、3.2）+ 带通俗副标题"X — 让 Y 做 Z"
4. **每章结尾给一张总图**：把本章所有子模块串起来
5. **mermaid 图密度高**：目标 15-20 张/30 页，混用 `flowchart TB/LR` + `sequenceDiagram` + `stateDiagram-v2`
6. **每张图配文字解读**：图不是装饰，是用来引出后面的解释
7. **大量用对比表格**：算法对比、参数表、cheatsheet — 目标 5-10 个表格
8. **代码片段要小、要可读**：5-15 行，必要时省略号 `# ...`，标注文件路径 `dimos/path/to/file.py:行号`
9. **关键概念用引用块强调**：`> **第一个关键点**：...`
10. **中英文混排**：技术术语保留英文（`Twist`、`OccupancyGrid`、`A*`），其他用中文
11. **加粗用于关键词**，斜体不用（中文不友好）
12. **章节标题里**用 `—`（em dash）做"主题 — 副标题"
13. **末尾必有版本注**：基于哪个 sha，便于日后对照

## Mermaid 用法约定

- **架构图**：`flowchart TB`，纵向流（上→下），用 `subgraph "层名"` 分组
- **数据流图**：`flowchart LR`，横向流（左→右），适合"输入→处理→输出"
- **多模块时序**：`sequenceDiagram`，参与者从左到右
- **状态机**：`stateDiagram-v2`，标注每个转移条件
- **节点标签里有中文 + `<br/>`**，比如 `[VoxelGridMapper<br/>累积 3D 体素]`
- **连线带标签**，比如 `A -->|cmd_vel| B`
- **避免一图超过 15 节点**，太大就拆成多张
- **mermaid.ink 的限制**：标签里**不要用裸的 `()`**（会被解析成节点形状），用 `（）` 中文括号或 `:` 分隔
- **subgraph 标题用引号**：`subgraph L1["第 1 层 — 感知"]`

## PDF 转换技术细节

工具脚本：`.cursor/scripts/md_to_pdf.py`

- 通过 `mermaid.ink` 在线服务**预渲染 SVG**（不用 mermaid.js 客户端渲染，避开 chrome `--print-to-pdf` 不等异步 JS 的坑）
- 用 chrome headless `--print-to-pdf`
- A4 页面，18mm 边距，限制 SVG `max-height: 600pt` 防止单图溢出页
- 中文字体：`Noto Sans CJK SC` / `PingFang SC` / `Microsoft YaHei`

**生成后必须验证**：

```bash
pdfinfo <topic>.pdf | head -5                       # 看页数和大小
pdftoppm -f 1 -l 2 -r 90 <topic>.pdf /tmp/check    # 抽 1-2 页
# Read /tmp/check-01.png 确认排版正常
pdftoppm -f <mermaid_page> -l <mermaid_page> ...    # 抽 mermaid 页确认图渲染了
```

如果 mermaid 页空白 → SVG 太大被裁，调整 CSS 的 `max-height` 或拆图。

## 篇幅参考

参考实现：`docs/cursor/dimos-navigation-mapping-tutorial.md`
- 35 KB markdown / 957 行
- 17 张 mermaid 图
- 9 个表格
- PDF: 31 页 / 1.8 MB
- 写作 + PDF 转换合计 ~1 次会话内完成

通常一份合格的 "dimos 模板" 文档应在 **20-50 KB markdown / 25-40 页 PDF** 范围。

## 调研流程（写之前必做）

1. 用 Glob 找出主题相关的所有源文件（`dimos/<area>/**/*.py`）
2. 并行 Read 关键模块（一次 3-7 个文件）
3. 重点看：模块的 `In/Out` 端口、`config` 字段、`@rpc`/`@skill` 方法、注释和 docstring
4. 调研完再开始写 — 不要边写边查
