[package]
name = "dimos-memory-recorder"
version = "0.1.0"
edition = "2021"
description = "Experimental native stream recorder for DimOS memory stores"
license = "Apache-2.0"

[dependencies]
anyhow = "1"
crossbeam-channel = "0.5"
crossbeam-utils = "0.8"
dimos-module = { path = "../../../../native/rust/dimos-module" }
lcm-msgs = { git = "https://github.com/dimensionalOS/dimos-lcm.git", branch = "rust-codegen" }
lz4_flex = "0.14.0"
mcap = { version = "0.25.0", default-features = false, features = ["zstd"] }
rayon = "1"
rusqlite = "0.40.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal"] }
tracing = "0.1"
turbojpeg = { version = "1.5.1", default-features = false, features = ["cmake"] }
validator = { version = "0.21", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
